import React from 'react';
import NavBar from './NavBar';
 
function Home(props) {
    // Could just be a simple landing page with news about new books added, library policy, etc.
    return (
        <div>
            <NavBar/>
            HOME PAGE
        </div>
    );
}
 
export default Home;
